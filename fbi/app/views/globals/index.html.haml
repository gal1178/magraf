%h1 Listing globals

%table
  %tr
    %th
    %th
    %th

  - @globals.each do |global|
    %tr
      %td= link_to 'Show', global
      %td= link_to 'Edit', edit_global_path(global)
      %td= link_to 'Destroy', global, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Global', new_global_path
